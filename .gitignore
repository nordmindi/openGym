# Exercise media — fetched on first `docker compose up` (or via scripts/fetch-media.sh)
media/img/
media/gif/

# Runtime data — users, passkeys, per-user state, session secret, VAPID keys
data/

# Local config
.env
frontend/android/keystore.properties
frontend/android/*.keystore
frontend/android/*.jks

# Build output / deps
frontend/dist/
node_modules/
*.log
.DS_Store
.claude/
.agents/

# CI reports
junit.xml
coverage/
frontend/bundle-size.json
scripts/.instr-tr.json
scripts/.instr-de-*.json
scripts/.instr-pt-*.json
